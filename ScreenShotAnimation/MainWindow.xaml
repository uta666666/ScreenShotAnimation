<Window
    x:Class="ScreenShotAnimation.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ScreenShotAnimation"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ScreenShotAnimation.ViewModel"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Button
            Width="116"
            Margin="499,364,0,35.667"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            Command="{Binding StartRecordingCommand}"
            Content="Start"
            IsEnabled="{Binding CanStartProcess.Value}" />
        <Button
            Width="109"
            Margin="641,364,0,35.667"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            Command="{Binding StopRecordingCommand}"
            Content="Stop" />
        <TextBox
            Width="469"
            Height="23"
            Margin="180,206,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Text="{Binding SavePath.Value}"
            TextWrapping="Wrap" />
    </Grid>
</Window>
